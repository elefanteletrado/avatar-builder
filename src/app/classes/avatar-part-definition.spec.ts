import { AvatarPartDefinition } from './avatar-part-definition';

describe('AvatarPartDefinition', () => {
  it('should create an instance', () => {
    expect(new AvatarPartDefinition()).toBeTruthy();
  });
});
